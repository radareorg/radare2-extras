bea:
	cd p ; $(MAKE) bin_dlang.$(LIBEXT)

spc700:
	cd p ; $(MAKE) bin_spc700.$(LIBEXT)

swf:
	cd p ; $(MAKE) bin_swf.$(LIBEXT)

pcap:
	cd p ; $(MAKE) bin_pcap.$(LIBEXT)

atombios:
	cd p ; $(MAKE) bin_atombios.$(LIBEXT)

clean:
	rm -f p/*.$(LIBEXT)

include ../../options.mk
