bea:
	cd p ; $(MAKE) bin_dlang.$(LIBEXT)

mdmp:
	cd p ; $(MAKE) bin_mdmp.$(LIBEXT)

pyc:
	cd p ; $(MAKE) bin_pyc.$(LIBEXT)

swf:
	cd p ; $(MAKE) bin_swf.$(LIBEXT)

pcap:
	cd p ; $(MAKE) bin_pcap.$(LIBEXT)

clean:
	rm -f p/*.$(LIBEXT)

include ../../options.mk
